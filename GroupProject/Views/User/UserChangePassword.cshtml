@using EntityLayer.Concrete
@model User
@{
    ViewData["Title"] = "UserChangePassword";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}
<div class="card shadow mb-4 col-lg-4">
    <div class="card-header py-3">
        <h5 class="m-0 font-weight-bold text-primary">Şifre Bilgisi Güncelleme Sayfası</h5>
    </div>
    <div class="card-body">
       
            @*@Html.HiddenFor(x => x.BookID, new { @class = "form-control" })
            @Html.HiddenFor(x => x.ReadBookID, new { @class = "form-control" })
            @Html.HiddenFor(x => x.Book.BookID, new { @class = "form-control" })
            <br />*@

            <div class="row pt-3">
                <div class="col-lg-12">
                    @Html.Label("Mevcut Şifre")
                  <input type="password" class="form-control"/>
                </div>
            </div>
            <div class="row pt-3">
                <div class="col-lg-12">
                    @Html.Label("Yeni Şifre")
                    <input type="password" class="form-control" />
                </div>
            </div>
            <div class="row pt-3">
                <div class="col-lg-12">
                    @Html.Label("Yeni Şifre Tekrar")
                    <input type="password" class="form-control" />
                </div>
            </div>
           
            <div class="pt-3">
                <button class="btn btn-success pr-4 pl-4 pt-2 pb-2" font-size: 20px"> <i class="fas fa-edit pr-3"></i>GÜNCELLE</button>
            </div>
        
    </div>
</div>